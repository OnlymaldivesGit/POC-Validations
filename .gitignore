# Ignore virtual environments and cache
myenv/
venv/
__pycache__/
*.pyc

# Ignore system files
.DS_Store
Thumbs.db

# Output binaries — stored via GitHub API, not committed directly
outputs/**/*.xlsx
outputs/**/*.html

# Keep the JSON index files
!outputs/index.json
!outputs/vendors.json
!outputs/.gitkeep
